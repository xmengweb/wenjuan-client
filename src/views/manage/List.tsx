import ListBar from "@/components/ListBar";
import { memo } from "react";

const List = memo(() => {
  return (
    <div>
      <ListBar name="问卷列表" />
      list
    </div>
  );
});

export default List;
